When reading a csv file, use os.path.join(sys.path[0], input()).
Create the required file and write the test files in a seperate test folder.
If dataset is not available create one.

If i ask u to create plots, use a ipynb and place the plot generation code for each graph in seperate cells.